import { PrivacySchema, type PrivacyType } from "./privacy";
import { TermsSchema, type TermsType } from "./terms";
import { WhoSchema, type WhoType } from "./who";

export { PrivacySchema, TermsSchema, WhoSchema };
export type { PrivacyType, TermsType, WhoType };
